version: 1
applications:
  - appRoot: miyog/frontend
    frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - npm run build
            # Copy static assets to standalone folder so everything is in one place
            - cp -r .next/static .next/standalone/miyog/frontend/.next/static
            - cp -r public .next/standalone/miyog/frontend/public
      artifacts:
        baseDirectory: .next/standalone/miyog/frontend
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
